# PROMPT — UI-4 Documents & AP/AR read (paste vào New Chat / Cloud Agent)

Base: UI-0–U3 trên `main`. Đọc `docs/sprint/PLAN-UI.md`.

## Mục tiêu
Chứng từ: Received ≠ Accepted ≠ Matched nhìn thấy; outstanding AP/AR đọc được (thin).

## DoD mỏng nhưng đủ
1. Document list/detail + receive/accept actions nếu API sẵn.
2. Bill hoặc party view: exposure / AP/AR outstanding (read).
3. Copy CP6.5 đúng thuật ngữ; không gộp Cost = Payment.
4. `docs/sprint/UI-4-DOD.md` + handoff + PR. Ghi follow-up match UI / settlement UI.

## Non-goals
Auto-match engine UI, bank feed, e-invoice provider screens.
